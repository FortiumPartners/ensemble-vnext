# Functional Success Definition: verify-functional-arg-guards

**Source**: docs/TRD/verify-functional-arg-guards.md §Reproduction
**Source kind**: reproduction
**Derived**: 2026-08-26T03:02:55Z
**Criteria**: 8

| ID | Functional statement | Cites | Evidence that would prove it | Derivation |
|----|----------------------|-------|------------------------------|------------|
| FS-1 | When `args.statePath` is omitted, the argument prelude throws an explicit `verify-functional: args.statePath is required …`-shaped error at the point `statePath` is unpacked — never a `TypeError: Cannot read properties of undefined (reading 'replace')` from downstream code that has nothing to do with the caller's mistake | §Reproduction Actual, line 18 (`statePath MISSING : TypeError: Cannot read properties of undefined (reading 'replace')`); derivation bullet lines 33-34 ("dereferenced by `.replace(/\/[^/]*$/, '')` at lines 91 and 98, so a missing value throws a `TypeError` at a line that has nothing to do with the caller's mistake"); production note lines 26-29 (Bun surfaced the same defect as `undefined is not an object (evaluating 'STATE_PATH.replace')` at `workflow.js:79`, "which names neither the argument nor the workflow," and blocked every `/verify-build` rerun in that session); Expected, lines 50-52 | Executing the prelude block (`packages/core/workflows/verify-functional.js:55-101`) against an `args` object with `statePath` omitted and every other required field present throws an `Error` whose message matches `/^verify-functional: args\.statePath is required/`, and no `TypeError` is thrown at the `STATE_DIR = STATE_PATH.replace(...)` line (currently line 91) | [read] |
| FS-2 | When `args.checker` is omitted, the argument prelude throws an explicit `verify-functional: args.checker is required …`-shaped error at the point `checker` is unpacked — never a `TypeError: Cannot read properties of undefined (reading 'replace')` | §Reproduction Actual, line 19 (`checker MISSING : TypeError: Cannot read properties of undefined (reading 'replace')`); derivation bullet lines 33-34 (same as FS-1, applies to both `statePath` and `checker`); Expected, lines 50-52 | Executing the prelude with `checker` omitted throws an `Error` matching `/^verify-functional: args\.checker is required/`, and no `TypeError` is thrown at the `LIB_DIR = CHECKER.replace(...)` line (currently line 98) | [read] |
| FS-3 | When `args.reportPath` is omitted, the run fails fast with an explicit `verify-functional: args.reportPath is required …`-shaped error — it must never proceed silently and hand a dispatched agent a prompt containing the literal string `undefined` in place of the report path | §Reproduction Actual, line 20 (`reportPath MISSING : no throw`); derivation bullet lines 42-46 ("never dereferenced in the prelude, so they pass silently and reach the model as the literal string `undefined` inside dispatched prompts (… `write the output to ${REPORT_PATH}` at line 220). This is the worse shape: the run proceeds and reports a result derived from a prompt that told an agent to look in a directory named `undefined`."); Expected, lines 50-52 | Executing the prelude with `reportPath` omitted throws an `Error` matching `/^verify-functional: args\.reportPath is required/`; no prompt string built later in the workflow (e.g. the "write the output to …" instruction) is ever constructed containing the literal substring `undefined` in place of the report path | [read] |
| FS-4 | When `args.evidenceDir` is omitted, the run fails fast with an explicit `verify-functional: args.evidenceDir is required …`-shaped error — it must never proceed silently and hand a dispatched agent a prompt containing the literal string `undefined` in place of the evidence directory | §Reproduction Actual, line 21 (`evidenceDir MISSING : no throw`); derivation bullet lines 42-46 ("never dereferenced in the prelude, so they pass silently and reach the model as the literal string `undefined` inside dispatched prompts (`Evidence directory: ${EVIDENCE_DIR}` at line 133 …)"); Expected, lines 50-52 | Executing the prelude with `evidenceDir` omitted throws an `Error` matching `/^verify-functional: args\.evidenceDir is required/`; no Exercise-stage prompt is ever built containing the literal substring `Evidence directory: undefined` | [read] |
| FS-5 | When `args.since` is omitted, the run fails fast with an explicit `verify-functional: args.since is required …`-shaped error at the point `since` is unpacked — it must never reach the Judge stage and surface later as a CLI usage error from a shell command a dispatched subagent is told to run | §Reproduction derivation bullet lines 36-41 ("a third shape … Unpacked bare at line 64 with no default and interpolated into a shell command the judge is told to run (`node ${CHECKER} check-evidence --file ${claimsFile} ${SINCE}` at line 174). The checker CLI does reject a non-finite value …, but it surfaces as a CLI usage error inside a dispatched subagent, which is precisely the diagnostic gap O1 exists to close."); Expected, lines 50-52 | Executing the prelude with `since` omitted throws an `Error` matching `/^verify-functional: args\.since is required/` before any Judge-stage prompt referencing `${SINCE}` (the `node ${CHECKER} check-evidence --file … ${SINCE}` instruction, currently built around line 174) is ever constructed or dispatched | [read] |
| FS-6 | With every required argument present (including `statePath`, `checker`, `reportPath`, `evidenceDir`, `since`, `criteria`, `cap`), the argument prelude does not throw | §Reproduction Actual, line 17 (`all args present : no throw`) | Executing the prelude against a fully-populated `args` object produces no thrown error | [read] |
| FS-7 | With `args.criteria` omitted, the prelude continues to throw exactly `Error: verify-functional: args.criteria is required and must be an array (possibly empty)` — the fix to the other arguments' guards must not regress this already-correct behavior | §Reproduction Actual, line 22 (`criteria MISSING (guard) : Error: verify-functional: args.criteria is required and must be an array (possibly empty)`); Expected, lines 50-52 ("the treatment `criteria` and `cap` already receive twelve lines above") | Executing the prelude with `criteria` omitted still throws `Error: verify-functional: args.criteria is required and must be an array (possibly empty)`, unchanged | [read] |
| FS-8 | With `args.cap` omitted, the prelude continues to throw exactly `Error: verify-functional: args.cap is required and must be a positive integer (the iteration cap, ordinarily 3)` — the fix to the other arguments' guards must not regress this already-correct behavior | §Reproduction Actual, line 23 (`cap MISSING (guard) : Error: verify-functional: args.cap is required and must be a positive integer (the iteration cap, ordinarily 3)`); Expected, lines 50-52 (same citation as FS-7) | Executing the prelude with `cap` omitted still throws `Error: verify-functional: args.cap is required and must be a positive integer (the iteration cap, ordinarily 3)`, unchanged | [read] |
