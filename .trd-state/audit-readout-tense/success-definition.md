# Functional Success Definition: audit-readout-tense

**Source**: docs/TRD/audit-readout-tense.md §Intended Change
**Source kind**: intended-change
**Derived**: 2026-09-25T00:00:00Z
**Criteria**: 12

| ID | Functional statement | Cites | Evidence that would prove it | Derivation |
|----|----------------------|-------|------------------------------|------------|
| FS-1 | When an audit command's "Apply what survives" pass deletes something from the document because nothing in the source asks for it, the readout labels that finding `DELETED`, never the imperative `DELETE` | Intended Change line 20 (table row: `DELETE` → `DELETED` — "the audit removes it") | Real proof requires an actual `/audit-prd`, `/audit-trd`, or `/audit-build` run that triggers a deletion finding, and reading its printed readout heading — a future model run, not yet observed. Nearest static evidence: the audit commands' readout-heading source text (their command prompts and/or the skill prompt they delegate to) contains the literal string `DELETED` for this finding type and no longer contains `DELETE` as a heading string | [read] |
| FS-2 | When the audit lowers a value to the constitution floor because no reason was given for exceeding it, the readout reads `LOWERED TO THE CONSTITUTION FLOOR — no reason was given for exceeding it`; when the TRD's author did state a reason for exceeding the floor, no finding of this kind is raised at all | Intended Change line 21 (table row: `LOWER TO THE CONSTITUTION FLOOR, or say why it's higher` → `LOWERED TO THE CONSTITUTION FLOOR — no reason was given for exceeding it`; "if a reason was given there is no finding. Always applied.") | Real proof requires an actual audit run against a TRD/PRD with an unexplained over-floor value (and, separately, one with a stated reason) and reading what the readout shows in each case — a future model run. Nearest static evidence: the audit prompt text contains the new heading string, the branch that suppresses the finding when a reason was given, and no longer contains the two-way imperative "LOWER TO THE CONSTITUTION FLOOR, or say why it's higher" | [read] |
| FS-3 | When the audit restores something that was in the source but missing from the document, the readout labels that finding `ADDED BACK`, never the imperative `ADD BACK` | Intended Change line 22 (table row: `ADD BACK` → `ADDED BACK` — "the audit restores it") | Real proof requires an actual audit run that triggers a restore finding, and reading its readout heading — a future model run. Nearest static evidence: the audit prompt text contains `ADDED BACK` for this finding type and no longer contains `ADD BACK` as a heading string | [read] |
| FS-4 | When the audit fixes a citation whose referenced ID does not resolve, the readout labels that finding `FIXED THE CITATION`, never the imperative `FIX THE CITATION` | Intended Change line 23 (table row: `FIX THE CITATION` → `FIXED THE CITATION` — "mechanical") | Real proof requires an actual audit run that triggers a citation fix, and reading its readout heading — a future model run. Nearest static evidence: the audit prompt text contains `FIXED THE CITATION` and no longer contains `FIX THE CITATION` as a heading string | [read] |
| FS-5 | When the audit corrects a stale claim in the document in favor of what the code actually does, the readout labels that finding `CORRECTED A STALE CLAIM`, never the assertion `THE DOC IS STALE` | Intended Change line 24 (table row: `THE DOC IS STALE` → `CORRECTED A STALE CLAIM` — "'the code wins' is settled policy, so the direction of correction is not a judgement") | Real proof requires an actual audit run that triggers a stale-claim correction, and reading its readout heading — a future model run. Nearest static evidence: the audit prompt text contains `CORRECTED A STALE CLAIM` and no longer contains `THE DOC IS STALE` as a heading string | [read] |
| FS-6 | A task the audit judges unbuildable-as-written now renders as one of two distinct headings depending on what the audit actually did: `REDESIGNED — could not be built as written` when the audit's own edit fixed it, or `CANNOT BE BUILT — needs a design decision` when fixing it would require inventing a replacement design decision rather than an edit — never the single undifferentiated `CANNOT BE BUILT AS WRITTEN` | Intended Change line 25 (table row: `CANNOT BE BUILT AS WRITTEN` "splits in two": `REDESIGNED — could not be built as written` / `CANNOT BE BUILT — needs a design decision` — "genuinely both-valued: sometimes an edit fixes it, sometimes inventing a replacement mechanism would be manufacturing design") | Real proof requires two actual audit runs — one where the audit's edit resolves an unbuildable task, one where it cannot without inventing a mechanism — and reading each readout's heading; a future model run in both cases. Nearest static evidence: the audit prompt text defines both new heading strings, conditioned on which of the two situations applies, and the old single heading `CANNOT BE BUILT AS WRITTEN` no longer appears | [read] |
| FS-7 | A contradiction the audit finds between two sources still renders as the imperative `PICK ONE — these contradict`, unchanged by this update, because resolving a contradiction is the owner's decision, not the audit's | Intended Change line 26 (table row: `PICK ONE — these contradict` → "unchanged" — "resolving a contradiction is the owner's") | Real proof requires an actual audit run that surfaces a contradiction, and reading that its readout heading still reads `PICK ONE — these contradict` — a future model run. Nearest static evidence: the audit prompt text still contains this exact heading string, unmodified by this change | [read] |
| FS-8 | A finding asking whether something is actually wanted still renders as the imperative `CONFIRM THESE ARE WANTED`, unchanged by this update, because that confirmation is the owner's to give | Intended Change line 27 (table row: `CONFIRM THESE ARE WANTED` → "unchanged" — "the owner's") | Real proof requires an actual audit run that surfaces this finding, and reading that its readout heading still reads `CONFIRM THESE ARE WANTED` — a future model run. Nearest static evidence: the audit prompt text still contains this exact heading string, unmodified by this change | [read] |
| FS-9 | In `/audit-prd` specifically, a requirement the audit finds already implemented still renders as the imperative `ALREADY BUILT`, unchanged by this update, because deciding whether the requirement survives is the owner's call | Intended Change line 28 (table row: `ALREADY BUILT` (`/audit-prd`) → "unchanged" — "deciding whether the requirement survives is the owner's") | Real proof requires an actual `/audit-prd` run that surfaces this finding, and reading that its readout heading still reads `ALREADY BUILT` — a future model run. Nearest static evidence: the `/audit-prd` prompt text still contains this exact heading string, unmodified by this change | [read] |
| FS-10 | This change adds no new heading that groups multiple finding types under a single umbrella label — the set of distinct headings an audit readout can show is unchanged in count except for the one split named in FS-6 | Intended Change line 16 ("No grouping headings are added") | Cannot be fully proven by a single run — it is a claim about what the prompt text does NOT add. Nearest evidence: a diff of the audit commands' readout-heading definitions before and after this change, confirmed to add no heading beyond the tense rewrites and the one two-way split, and no wrapping/grouping heading around them | [read] |
| FS-11 | For the finding the source refers to as O3, the readout reuses the same heading spelling the already-passing (clean) path emits — `CAVEAT` — and states a one-line pointer to the document's own `## Could Not Verify` section (e.g. "CAVEAT — 11 claims went unchecked; see the TRD's ## Could Not Verify") rather than reprinting that section's contents inline in the readout | Intended Change lines 30–33 ("the readout reuses the spelling the clean path already emits — `CAVEAT` — and points at the document rather than reprinting it") | Real proof requires an actual audit run that leaves some claims unchecked, and reading that its readout shows the `CAVEAT` line pointing at `## Could Not Verify` rather than reproducing its contents — a future model run. Nearest static evidence: the audit prompt text emits exactly this pointer form for this finding, not the section's contents | [read] |
| FS-12 | Across every finding type an audit readout can show, a heading describing something the audit itself already did reads in the past tense, and a heading describing something only the owner can decide stays in the imperative — this general rule, not just the specific headings enumerated in FS-1–FS-9, holds for whatever the audit prompts actually define | Intended Change lines 15–16 ("a heading applied by the audit is past tense; a heading needing the owner stays imperative") | Cannot be fully proven by grepping for individual strings, since it is a rule about every heading, not a fixed list. Nearest evidence: a full read of the audit commands' readout-heading definitions confirming every audit-performed action uses past tense and every owner-decision heading stays imperative, with no exceptions beyond what FS-1–FS-9 already enumerate; full confidence ultimately requires observing readouts across real runs covering each finding type — a future model run | [read] |
